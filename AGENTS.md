# Project Guidelines

## Building
To build the project, use the `docker-build.py` script with the `-b` flag to select a target. Use `-c` to clean build and `-v` for verbose output. Use `--help` if you need more info.
